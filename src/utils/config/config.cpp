#include "utils/config/config.hpp"

// ========================
// Config Implementation
// ========================

// TODO: Implement load()
// Step 1 → Open file.
// Step 2 → Parse lines as "key=value" pairs.
// Step 3 → Store in 'values' map.
bool Config::load(const std::string& filename) {
    // TODO: Add implementation later
    return false;
}

// TODO: Implement get()
// Step 1 → Look up key in map.
// Step 2 → Return associated value or empty string if not found.
std::string Config::get(const std::string& key) const {
    // TODO: Add implementation later
    return {};
}
